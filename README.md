# Teraflock Homebrew Tap

```sh
brew install teraflock/tap/flock
```

Installs `flockd` (the node daemon) and `flock` (CLI/TUI). Formulae are
published automatically by goreleaser from teraflock/flockd releases.
